/** Hello World program. */
class Main {
   function void main() {
      /* Prints some text using the standard library. */
      do Theme.draw();
      do Theme.splash();
      do Keyboard.readChar();
      do Screen.clearScreen();
      do Sys.halt();
      return;
   }
}
